@model AppUser
@{
    Layout = "_AdminLayout";
}

<div class="row">
    <div class="col">
        <div class="card">
            <div class="card-body  text-center">
                <p>"@Model.AdSoyad" adlı kullanıcıyı silmek istediğinize emin misiniz?</p>
                <form method="post" asp-controller="User" asp-action="DeleteConfirm">
                    <input type="hidden" asp-for="Id" value="@Model.Id" />
                    <button type="submit" class="btn btn-danger">Sil</button>
                    <a asp-controller="User" asp-action="Index" class="btn btn-primary">İptal</a>
                </form>
            </div>
        </div>
    </div>
</div>